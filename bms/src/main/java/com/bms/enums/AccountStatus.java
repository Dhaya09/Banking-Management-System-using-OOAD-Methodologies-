package com.bms.enums;

/**
 * UML: State Machine — Account States
 * Created → Active → Frozen → Dormant → Closed → Overdrawn
 */
public enum AccountStatus {
    CREATED, ACTIVE, FROZEN, DORMANT, CLOSED, OVERDRAWN
}
